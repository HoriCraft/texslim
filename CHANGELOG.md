# Changelog

## 1.0.0 — 初回公開リリース

一般公開する最初のバージョンです。
0.1.0 / 0.2.0 は公開前の内部版で、外部には配布していません。

### 主な内容

- テクスチャのインポート設定だけを変更する**非破壊の圧縮**
- 圧縮前の設定を `ProjectSettings/TexSlim.json` に記録し、**いつでも完全復元**
  （テクスチャ1枚 / このアバター分 / プロジェクト全体 の3系統）
- 顔・瞳・髪の**自動保護**と、キーワードの追加・例外指定
- 圧縮モード3種（解像度＋Crunch / Crunchのみ / 解像度のみ）
- テクスチャ1枚ずつの対象指定・解像度指定、重い順の並べ替え
- ダウンロードサイズとテクスチャメモリの**実測表示**
- 日本語 / 英語の切り替え
- VPM（VCC）での配布

### 既定値

- 最大サイズ **1024**（2048 では元から小さいテクスチャが縮まず、効果が出にくいため）
- 詳細タブの表示形式は **リスト**（重いテクスチャから潰す使い方を想定）

---

## 公開前の内部版（参考）

### 0.2.0（内部）

圧縮方式を全面的に変えました。0.1.0 とは互換性がありません。

- **ツール名を TexSlim（テックスリム）に変更。**
  パッケージ ID は `com.horicraft.texslim`、名前空間は `HoriCraft.TexSlim`。
  記録ファイルも `ProjectSettings/TexSlim.json` に変わりますが、
  旧ファイルが残っていれば起動時に自動で引き継ぎます。
- **圧縮方式をインポート設定の変更方式に置き換え。**
  `_Compressed` のコピーもマテリアルの複製も作らなくなりました。テクスチャアセットそのものの
  Max Size / Crunch 設定を変えるため、そのテクスチャを使う全マテリアル・全シーンに反映されます。
  元の画像ファイル（.png 本体）は無変更です。
- **復元記録をプロジェクト全体の1ファイルへ移動。**
  `ProjectSettings/TexSlim.json` に GUID をキーとして圧縮前の設定を記録します。
  初回のみ記録して上書きしないため、何度圧縮し直しても本当の原本へ戻せます。
  コンポーネントに記録していた 0.1.0 では、別シーンから同じテクスチャを開くと原本を辿れませんでした。
- **効果の表示を実測値に修正。** 0.1.0 では PNG のバイト数で測っていたため、常に「-0%」と表示されていました。
  ダウンロードサイズとテクスチャメモリを実測して出します。
- **合計をアバター全体で表示。** 保護中のテクスチャを含まない合計だったため、実際より小さく見えていました。
- UI の用語を統一し、Unity の内部用語（Renderer、台帳、取り込み設定など）を減らしました。
- エディタフォントに存在しない絵文字を廃止しました（環境によって何も描画されないため）。

### 追加

- 復元を3系統に整理（テクスチャ1枚 / このアバター分 / プロジェクト全体）
- 圧縮モードを3種類に（解像度＋Crunch / Crunchのみ / 解像度のみ）
- テクスチャごとの個別サイズ指定。圧縮済みの行でサイズを選び直すと、自動で戻して再圧縮します
- 検索と状態フィルタ（対象／圧縮済／保護／除外／圧縮不可）
- ツリー表示とリスト表示の切り替え、VRAM の大きい順ソート
- 保護キーワードの追加と「保護の例外」（部分一致の誤爆を打ち消す語）
- 日本語／英語の切り替え（ヘッダーに常時表示）
- 10枚ごとのチャンク処理とキャンセル可能な進捗バー
- `VRC.SDKBase.IEditorOnly` の実装（アップロード後のアバターに残らない）

### 修正

- 保護キーワードの判定単位が表示とズレており、「保護」表示のテクスチャが実際には圧縮されることがあった
- 圧縮後に保護を ON にすると、戻すボタンごと消えて復元できなくなっていた
- 復元後も「圧縮済」バッジが残っていた
- 一覧の行クリックに除外が割り当たっており、展開しようとすると設定が変わっていた
- `Crunchのみ` モードで効かない最大サイズが、そのまま操作できてしまっていた
- 初期の保護キーワードが組み込みと重複しており、顔・瞳の保護を OFF にしても解除されなかった
  （旧データは初回起動時に自動移行します）

## 0.1.0

- 初回リリース。
